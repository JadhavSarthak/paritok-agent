# paritok-agent
TokenLean Agent is a coding agent that uses Paritok to prune irrelevant context before every LLM call, keeping token usage flat instead of growing with each step — with a live dashboard showing real, measured savings against a naive baseline
